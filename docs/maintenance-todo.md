# 维护 TODO

## 交互行为

- [x] 主界面既有按钮复用统一鼠标状态。
- [x] 搜索、cURL 字段、列表和表格复用对应布局命中区域。
- [x] 滚动条点击与拖拽使用统一滚动状态。
- [x] 场景选择和响应操作菜单复用下拉状态的命中、偏移和悬停处理。
- [x] 下拉菜单触发按钮在左键释放时切换打开状态。

## 布局与状态

- [x] 搜索处理复用调用方已计算的屏幕布局。
- [x] cURL 字段命中与光标定位复用同一组区域。
- [x] 页面布局函数使用当前页面语义命名。
- [x] 工作区选择器复用过滤结果和布局计算。
- [x] 变量页、配置编辑和提示框沿用现有键盘操作。
- [x] 变量页和提取顺序页复用同一列表页布局、表格样式、滚动条点击与拖拽处理。
- [x] 整页内容状态由同一处判断。

## 代码结构

- [x] UI 模块声明直接依赖。
- [x] 保留现有交互、键盘快捷键和错误处理行为。
- [x] 文本窗口与窗口列换算使用同一组函数。
- [x] 各页光标定位使用统一的区域辅助函数。
- [x] cURL 字段的值、光标和聚焦状态由同一处构造。
- [x] 表格行编辑单元格的文本与样式判断合并为单个编辑器绑定。
- [x] 文本窗口的片段截取和字符宽度由同一组函数处理。
- [x] cURL 页的字段查找、取值和光标位置由同一处匹配。
- [x] 会话提取顺序的读取与写入使用同一组方法。

## 验证

- [x] `cargo fmt --all -- --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo build`
- [x] `git diff --check`
- [x] 临时配置下完成一次鼠标交互流程并清理临时文件。
