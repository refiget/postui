# 维护 TODO

## 交互行为

- [x] 主界面移除按钮控件，动作改为键盘触发。
- [x] 响应格式切换绑定 `f`，响应放大绑定 `z`。
- [x] 容器切换绑定 `Tab`/`Shift+Tab` 和 `Ctrl+N`/`Ctrl+P`。
- [x] cURL 导入移除按钮，`Ctrl+S` 导入、`Esc` 取消、`Tab` 切换字段。
- [x] 搜索、cURL 字段、列表和表格复用对应布局命中区域。
- [x] 滚动条点击与拖拽使用统一滚动状态。
- [x] 场景选择和响应操作菜单复用下拉状态的命中、偏移和悬停处理。
- [x] 下拉菜单由键盘打开，点击菜单外关闭。
- [x] 请求体页签的 `j`/`k` 在字段之间移动光标，`Enter` 编辑光标所在字段。
- [x] 请求体页签的字段光标覆盖请求体 JSON 的值、表单字段值和文件路径，鼠标点击复用同一套光标定位。
- [x] `e` 用外部编辑器打开当前接口的源文件，退出码为 0 时自动重新读取工作区。

## 布局与状态

- [x] 搜索处理复用调用方已计算的屏幕布局。
- [x] cURL 字段命中与光标定位复用同一组区域。
- [x] 页面布局函数使用当前页面语义命名。
- [x] 工作区选择器复用过滤结果和布局计算。
- [x] 变量页、配置编辑和提示框沿用现有键盘操作。
- [x] 变量页复用列表页布局、表格样式、滚动条点击与拖拽处理。
- [x] 整页内容状态由同一处判断。
- [x] 场景名和变量数量在顶栏以文本展示。
- [x] 请求容器的编辑对象、输入和渲染位置保存在同一个编辑器状态里。
- [x] 按键归属由单个层判断决定，按键提示和按键表由同一处映射。

## 代码结构

- [x] UI 模块声明直接依赖。
- [x] 焦点状态只保留 Header、Requests、Preview、Response。
- [x] 文本窗口与窗口列换算使用同一组函数。
- [x] 各页光标定位使用统一的区域辅助函数。
- [x] cURL 字段的值、光标和聚焦状态由同一处构造。
- [x] 表格行编辑单元格的文本与样式判断合并为单个编辑器绑定。
- [x] 文本窗口的片段截取和字符宽度由同一组函数处理。
- [x] cURL 页的字段查找、取值和光标位置由同一处匹配。
- [x] 内容页签的字段定位、取值和提交按编辑对象分派。
- [x] 内容页签的编辑器绘制使用同一处区域换算。
- [x] 内容页签字段的编辑对象和当前值由同一处匹配。
- [x] 参数和请求头表格的行数、选中行、滚动和编辑器由同一处读取。
- [x] 参数和请求头表格的列位置和删除列由同一处换算。
- [x] 参数和请求头表格的选中行、选中列、滚动和编辑器由同一处状态持有，两次表格绘制合并为单个函数。
- [x] 请求体各部分的文本拼接由同一处函数处理。
- [x] 键盘和滚轮的滚动步长由单个常量决定，三种滚动状态共用同一步长换算。
- [x] 场景名和空场景集合由单个常量与构造函数提供。
- [x] 本地引用检出和助手工具目录不进入版本库。
- [x] 错误页和请求列表的外部编辑器请求由同一处状态和同一处退出处理。
- [x] 工作区配置文件名为单个常量，加载、重载和错误页共用。
- [x] 工作区目录名为单个常量，发现和解析工作区共用。

## 验证

- [x] `cargo fmt --all -- --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo build`
- [x] `git diff --check`
- [x] 临时配置下完成一次发送、放大、格式切换和 cURL 导入流程并清理临时文件。
- [x] 临时配置下完成请求体 JSON 值、表单字段和文件路径的编辑与发送，参数表格开关，响应搜索，鼠标点击与双击编辑流程并清理临时文件。
- [x] 临时对照下完成参数与请求头表格的键盘、鼠标和渲染检查并清理临时文件。
