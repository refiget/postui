# Optional user interface configuration.
# Linux: ${XDG_CONFIG_HOME:-$HOME/.config}/postui/config.yaml
# Windows: %APPDATA%\postui\config.yaml
# Load another file with postui --config <path>. All fields are optional.
language: en
theme: gruvbox-dark
# Maximum response bytes available to formatting and display. Default: 16 MiB.
# max_response_display_bytes: 16777216
