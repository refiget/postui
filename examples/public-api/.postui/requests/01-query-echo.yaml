name: 查询参数回显
description: 同一份 GET 请求在两个场景中使用不同的公开 API 地址和场景变量。
method: GET
url: "{{api_base}}/get"
params:
  - name: scenario
    value: "{{scenario}}"
  - name: client
    value: "{{client_name}}"
  - name: request_id
    value: "{{request_id}}"
extracts:
  - variable: echoed_url
    path: url
