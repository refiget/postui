name: Create task
description: JSON body with workspace variables.
method: POST
url: http://{{host}}/v1/tasks
headers:
  X-Debug-Token: "{{token}}"
body: '{"taskId":"{{task_id}}","name":"{{item_name}}","value":"{{item_value}}"}'
