name: Create task
description: JSON body and variable extraction.
method: POST
url: http://{{host}}/v1/tasks
headers:
  X-Debug-Token: "{{token}}"
body: '{"taskId":"{{task_id}}","name":"{{item_name}}","value":"{{item_value}}"}'
extracts:
  - variable: task_id
    path: data.taskId
